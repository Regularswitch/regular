# Credenciais só para captura local (não versionar — use .env)
# Copie: cp .env.example .env

WP_ADMIN_URL=http://regularswitch-wp.local
WP_USER=admin
WP_PASS=

# Opcional: IDs conhecidos (senão o script tenta achar o primeiro post)
# WP_INTRO_ID=
# WP_CAPABILITIES_ID=
# WP_PROJECT_ID=
# WP_EDUCATION_ID=
